# Local Unix-socket connections require password authentication.
local   all             all                                     scram-sha-256

# TCP connections from localhost require SCRAM authentication.
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Local Docker installations may allocate any RFC1918 subnet. This file is
# development-only; loopback port publishing and SCRAM authentication still apply.
host    all             all             10.0.0.0/8               scram-sha-256
host    all             all             172.16.0.0/12            scram-sha-256
host    all             all             192.168.0.0/16           scram-sha-256

# Replication connections also require SCRAM authentication.
local   replication     all                                     scram-sha-256
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256
